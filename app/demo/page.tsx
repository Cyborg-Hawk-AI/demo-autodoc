import { DemoApp } from "@/components/demo/DemoApp";

export const metadata = {
  title: "Live Demo — AutoDoc",
  description: "Interactive mock of AutoDoc workflow documentation platform",
};

export default function DemoPage() {
  return <DemoApp />;
}
